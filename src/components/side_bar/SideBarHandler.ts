import type { Note } from "../Features/Note";
//選択されたものを削除する
export function deleteHandler(selectedIds:string[],notes:Note[]){
    // if(selectedIds.length === 0)return;//選択されたものがなかった場合
    // notes.map({note () => selectedIds.
    // })

}